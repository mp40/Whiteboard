const {primeFactors, countFactors} = require('./primeFactors')

describe("counting factors of a given number",()=>{
    it('should return the number of factors of the parameter',()=>{
        //expect(countFactors(24)).toBe(8)
    })
})