const BinarySearchTree = require('./bst')

describe.skip('Binary Search Tree construction', () => {
    const testTree = new BinarySearchTree(10);
    // it('should have a working insert method', () => {
    //     expect(testTree.insert(5)).toBe(true)
    // })
    it('should have one test', () => {
        expect(true).toEqual(true)
    })
})