function split(num,len){
    return num > 0 && num < len ? true : false
}

function calculateDiff(tape, total){
    return (total-tape)-tape
}

function solution(A) {
    // write your code in JavaScript (Node.js 8.9.4)

}

module.exports = {solution, split, calculateDiff};