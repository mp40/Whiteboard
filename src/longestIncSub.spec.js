const findSub = require('./longestIncSub')

describe('longest incrasing subsequence',()=>{
    // const arr = [10,3,7,9,0,15]
    // it('should return array, first element is length of subarray',()=>{
    //     expect(findSub(arr)).toBe(2)
    // })
    it('should pass',()=>{
        expect(true).toBe(true)
    })
    
})